class AutomatedCompliance::AlrValidator < AutomatedCompliance::Base
  def call(permit_application)
    #validates if a user is on ALR given its coordinates
    #update fields related to the ALR based on the customCompliance on the field
  end
end
