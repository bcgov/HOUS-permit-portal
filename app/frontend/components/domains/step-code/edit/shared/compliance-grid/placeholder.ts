import { GridItem, styled } from "@chakra-ui/react"

export const GridPlaceholder = styled(GridItem)
GridPlaceholder.defaultProps = {
  bg: "greys.grey04",
  borderTopWidth: 1,
  borderColor: "borders.light",
}
