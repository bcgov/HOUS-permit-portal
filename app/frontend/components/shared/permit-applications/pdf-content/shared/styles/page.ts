export const page = {
  marginLeft: "auto",
  marginRight: "auto",
  padding: 60,
  // for some reason, the content tends to get cut off at the bottom of the page unless we add this negative padding
  paddingBottom: -60,
}
